// Orden de las operaciones

let resultado;

resultado = 20 + 30 * 2; //El orden es igual que la matematica que conocemos habitualmente.
resultado = (20 + 12) / 2; //Los parentesis tienen prioridad


console.log(resultado);